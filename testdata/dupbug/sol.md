name: sol
role: settings-ui

Filing a settings bug: after I switch UI languages at runtime — I added English
GB, then Czech, reopening the menu in between — a button label in settings shows
up broken and mangled. It only goes wrong once I've changed languages without
restarting.

Screenshot is in the report. Expected the label to just re-localize.
