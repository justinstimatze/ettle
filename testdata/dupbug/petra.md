name: petra
role: graphics-text

Filing a tab crash I can reproduce every time: searching Google for "calibri
font" or "impact font" crashes the tab. The non-text parts of the page load,
then it dies — seems tied to rendering the font previews on the results page.
On Linux.

Repro is just running those two searches. Happy to grab a stack if someone
points me at the right build.
