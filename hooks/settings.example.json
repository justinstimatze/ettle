{
  "//": "ettle pull hook — see hooks/README.md. Merge these into your Claude Code settings (~/.claude/settings.json for all projects, or a project's .claude/settings.json). Replace YOUR_ROOM with your Linear room, and adjust the PostToolUse matcher to your Linear MCP server's tool prefix.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          { "type": "command", "command": "ettle pull-hook --room YOUR_ROOM" }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "mcp__linear",
        "hooks": [
          { "type": "command", "command": "ettle pull-hook --room YOUR_ROOM" }
        ]
      }
    ]
  }
}
